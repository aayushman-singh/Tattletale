# Codex review — codex\20260608-203646-final

_Scrubbed to findings only; the raw diff (which contained pre-sanitisation
secrets in `-` lines) was removed. Model: gpt-5.5, xhigh reasoning._

---

**Findings**

Critical: CI is fake. `.github/workflows/ci.yml:22`, `:40`, `:54`, `:64` uses `npm ci || npm install`, `tsc || true`, and `ruff ... || true`. This guarantees green checks even when lockfiles, typecheck, or lint are broken. For a hardening branch, this is disqualifying.

Critical: the backend JWT secret is still hardcoded. `backend/__tests__/users.test.js:28` explicitly documents `"1234"` as the shared secret. `.env.example` and `docker-compose.yml` pretend `JWT_SECRET` matters, but the app still signs/verifies with a known secret. Any token can be forged.

Critical: replay mode is not wired to `REPLAY_MODE`. `.env.example` claims `REPLAY_MODE=1` makes the scraper read fixtures, but `scraper/src/replay/cli.ts:10-15` says the CLI runs regardless and the platform routes never check the flag. Setting replay mode and running a scraper can still hit live platforms.

Critical: the public demo likely 404s. `frontend/src/components/demo/index.jsx:47-63` fetches `/demo/report.json`, `/demo/manifest.json`, `/demo/custody-log.json`, `/demo/report.pdf`, but `scraper/src/replay/cli.ts:31-35` writes to `output/golden/<handle>/generated`. There is no copy/build path to `frontend/public/demo`.

Critical: the chain-of-custody story is mathematically inconsistent. `runReplay.ts:47-65` renders the PDF with a provisional report-only chain, then computes the final chain after the PDF exists. The PDF cannot contain the final root hash because the PDF hash is part of that root. Also `manifest.json` and `custody-log.json` are not included in the custody chain despite the UI presenting them as real artifacts. This is exactly the kind of evidence-handling claim a senior reviewer will shred.

Critical: no signature, no external anchor, no append-only enforcement. `custody.ts` is a hash chain in a mutable JSON file. Anyone can tamper with artifacts and regenerate the manifest. Calling this “court-ready” or “chain of custody” without signing/anchoring is overclaiming.

Critical: secret removal is incomplete as shown. `DECISIONS.md:19` lists tracked `.env`, `session.json`, and `scraper/session.json`, but the diff only deletes `google.json`. If those files remain tracked, the branch failed its first job.

High: the PR diff itself still exposes deleted live-looking secrets in removed lines: Atlas URI, Google OAuth secret, and Instagram credentials. Do not show this PR/diff to a hiring manager. Use a clean history/squashed fresh repo after rotation.

High: backend tests lock in broken behavior instead of hardening it. `backend/__tests__/users.test.js:10-19` and `:75-82` normalize Express default HTML errors and 500s for validation failures. That is not a test suite; it is a bug preservation harness.

High: Docker backend healthcheck is wrong. `docker/Dockerfile.backend:14-15` probes `/api/users`, which is protected and should return 401, so the container will be unhealthy. It also copies only `package.json` and runs `npm install`, not `npm ci`.

High: `docker-compose.yml:29` has `JWT_SECRET: ${JWT_SECRET:-dev-only-insecure-change-me}`. That is a fallback default secret in a production-labeled service. It directly violates the stated no-fallback security posture.

High: backend test tooling appears to be production dependencies. `backend/package.json` adds Jest, Supertest, cross-env, and mongodb-memory-server alongside runtime deps. The production image will install test infrastructure unless package structure is fixed.

High: scraper package scripts still point at old nonexistent capitalized entrypoints. `scraper/package.json:37-40` still uses `src/Instagram.ts`, `src/Facebook.ts`, `src/X.ts`, while `vercel.json` was changed to lowercase route files. Local commands remain broken.

High: Python report scripts remain broken. `frontend/pdf_conv/facebook.py`, `instagram.py`, and `telegram.py` still assign `DATABASE_NAME=YOUR_DATABASE_NAME_HERE` and `COLLECTION_NAME=YOUR_COLLECTION_NAME_HERE` as bare undefined names. CI would catch this, except lint is allowed to fail.

High: most scraper routes now depend on `MONGO_CLUSTER_URI` but do not load dotenv. `instagram.ts`, `discord.ts`, `facebook.ts`, `x.ts`, `youtube.ts`, `whatsapp.ts`, `timeline.ts`, and `google.ts` import `clusterUri()` but not the repo config loader. Local `.env` use is likely broken.

Medium: `frontend/src/App.jsx:77-98` removes `GoogleOAuthProvider` entirely when the env var is missing. Any rendered Google login component using provider context can crash. Demo mode should gate Google controls, not change the provider topology globally.

Medium: `mobileScraper/test.ts:42` ends with `.catch(console.error)`, which logs failure but can exit successfully. That is silent failure in script form.

Medium: `requirements.txt` now says Maigret must be installed separately. That kills reproducible setup. A portfolio repo should not require readers to infer missing runtime dependencies from comments.

Medium: `HANDOFF.md`, `STATE.md`, and `DECISIONS.md` read like agent scratchpads, not portfolio documentation. They advertise compromised history, internal automation process, and unfinished work. Move the useful parts into `SECURITY.md`/ADRs and delete the orchestration residue.

**Verdict: NO-GO**

Do not show this to a hiring manager. The branch still has fake CI, broken demo wiring, hardcoded auth secrets, incomplete secret cleanup, and an evidentiary hashing claim that does not hold up technically.
