# Codex review — codex\20260608-203853-final

_Scrubbed to findings only; the raw diff (which contained pre-sanitisation
secrets in `-` lines) was removed. Model: gpt-5.5, xhigh reasoning._

---

**Findings**

Critical: the diff still exposes the removed secrets. Deleted lines in the patch contain the actual Atlas URI/password, Google OAuth secret, and Instagram credentials. If this appears in a PR, review artifact, public branch, or saved codex review, the leak continues. Rotation plus history scrub must happen before this is portfolio-safe.

Critical: backend auth still appears to use hardcoded JWT secret `1234`, and the new tests depend on that. `JWT_SECRET` in `.env.example` and Docker is theater if token generation/middleware ignore it. This is a security hard-stop.

Critical: CI is not trustworthy. `npx tsc --noEmit || true` and `ruff check ... || true` guarantee green builds while typecheck/lint can fail. `npm ci || npm install` hides lockfile/reproducibility problems. A hardening branch with intentionally non-failing checks is worse than no CI.

Critical: the replay demo is disconnected. The frontend fetches `/demo/report.json`, `/demo/manifest.json`, `/demo/custody-log.json`, and `/demo/report.pdf`, but the diff does not add those files under `frontend/public/demo`. The replay CLI writes to `output/golden/<handle>/generated`, not the frontend path. The public “Run demo case” button will likely fail.

High: the SHA-256 chain-of-custody is not defensible. The PDF is rendered with a provisional chain before the PDF hash exists, while the final root hash includes the PDF. So the PDF cannot embed the same final custody chain shown by the UI. The manifest and custody log themselves are not hashed or signed, and there is no external trust anchor. This is a demo hash chain, not court-grade custody.

High: `.env.example` says `REPLAY_MODE=1` makes the scraper read golden fixtures, but the live scraper routes do not switch on `REPLAY_MODE`. Only a separate CLI exists. That is a contract lie in the docs/config.

High: backend tests codify broken behavior instead of hardening it: missing required fields can return 500, Express default HTML errors are expected, invalid auth returns 400, and there is still no centralized error handler, input validation, rate limiting, or meaningful CORS policy.

High: several Python report scripts are still broken or suspicious. `facebook.py`, `instagram.py`, and `telegram.py` still contain unquoted `YOUR_DATABASE_NAME_HERE` / `YOUR_COLLECTION_NAME_HERE`, which are import-time `NameError`s. The dependency trim is also unverified because Python CI is allowed to pass even when lint fails and there is no import smoke test.

High: scraper environment loading is inconsistent. Most TS routes now call `clusterUri()`, but only `mastodon.ts` and `log.ts` visibly import the config loader. If `.env` is the expected local path, many routes will fail loudly even when `.env` exists.

High: Docker is probably not healthy. The backend healthcheck calls `/api/users`, which is a protected route and likely returns 401, making the container unhealthy. Docker builds are not run in CI, and both Dockerfiles use non-reproducible `npm install` patterns.

High: scraper scripts and deploy config still smell broken. `start` points to `start:dev`, which is not defined. The package scripts still reference capitalized `src/Instagram.ts` / `src/X.ts` paths while the Vercel config was changed to lowercase route files. `scraper/vercel.json` says scraper is not deployable but still keeps a deployment config pretending to be fixed.

Medium: the Instagram route still preserves a commented-out core scrape path and replaces behavior with a “future work” comment. For this branch’s stated purpose, documenting a no-op is not fixing the broken path.

Medium: `HANDOFF.md`, `STATE.md`, and `DECISIONS.md` are not portfolio-grade public artifacts. They expose internal agent workflow, incident details, unresolved secret history, and sensitive claims like a production fork for an agency. Keep sanitized public docs; keep agent scratchpads out of the repo.

Medium: frontend demo has no tests and can break outside `/demo`. Removing `GoogleOAuthProvider` when the env var is missing may crash auth pages if any descendant uses Google OAuth hooks/components. The warning also fires during render.

Medium: there is no secret-scanning gate. For a branch whose first job is secret removal, CI should run something like gitleaks/trufflehog and fail hard.

Verdict: **NO-GO** for showing to a hiring manager.
