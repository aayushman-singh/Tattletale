# Codex review — pass 2 (second hardening pass)

_Findings only; model gpt-5.5 xhigh._

---

codex
Findings:

1. `scraper/src/routes/x.ts`: the dotenv side-effect import is after `mongoUri.js`. If `mongoUri.js` reads env at module evaluation time, this still fails exactly like before. Put `import "../../../config.js";` first.

2. `.github/workflows/ci.yml`: `npm install` in CI is honest but not reproducible. If lockfiles are absent, this is acceptable short-term, but it means CI can pass against dependency versions nobody committed. Embarrassing, not blocking.

3. `secret-scan`: working-tree gitleaks is now blocking and no longer hidden by `|| true`, but the curl-piped binary install is unauthenticated beyond TLS. Better to use a pinned Action or verify checksum. Not a functional blocker.

4. `backend/__tests__/setup.js`: `process.env.JWT_SECRET = process.env.JWT_SECRET || 'test-secret'` is fine for tests, but it weakens fail-loud semantics inside tests if a caller intentionally sets an empty value. Minor.

Prior criticals: JWT hardcode appears genuinely resolved in token generation and auth middleware; the secret is no longer logged. CI fake-green is mostly resolved, with scraper typecheck honestly marked non-blocking. Python NameErrors are fixed at syntax/name level. Docker healthcheck now hits a public endpoint. Signup missing-field crash is fixed. Replay/doc honesty is materially improved.

The one thing I would not wave through without checking is the `x.ts` import order, because it may mean the “missing dotenv” fix is only cosmetic.

REMAINING BLOCKERS: scraper/src/routes/x.ts dotenv import may execute too late; residual = git-history secret rotation (user action)
tokens used
20,480
Findings:

1. `scraper/src/routes/x.ts`: the dotenv side-effect import is after `mongoUri.js`. If `mongoUri.js` reads env at module evaluation time, this still fails exactly like before. Put `import "../../../config.js";` first.

2. `.github/workflows/ci.yml`: `npm install` in CI is honest but not reproducible. If lockfiles are absent, this is acceptable short-term, but it means CI can pass against dependency versions nobody committed. Embarrassing, not blocking.

3. `secret-scan`: working-tree gitleaks is now blocking and no longer hidden by `|| true`, but the curl-piped binary install is unauthenticated beyond TLS. Better to use a pinned Action or verify checksum. Not a functional blocker.

4. `backend/__tests__/setup.js`: `process.env.JWT_SECRET = process.env.JWT_SECRET || 'test-secret'` is fine for tests, but it weakens fail-loud semantics inside tests if a caller intentionally sets an empty value. Minor.

Prior criticals: JWT hardcode appears genuinely resolved in token generation and auth middleware; the secret is no longer logged. CI fake-green is mostly resolved, with scraper typecheck honestly marked non-blocking. Python NameErrors are fixed at syntax/name level. Docker healthcheck now hits a public endpoint. Signup missing-field crash is fixed. Replay/doc honesty is materially improved.

The one thing I would not wave through without checking is the `x.ts` import order, because it may mean the “missing dotenv” fix is only cosmetic.

REMAINING BLOCKERS: scraper/src/routes/x.ts dotenv import may execute too late; residual = git-history secret rotation (user action)
