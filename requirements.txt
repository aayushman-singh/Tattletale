# Tattletale Python dependencies — the scraper + PDF report generators + chatbot.
#
# This list is curated to ONLY what the Python code actually imports
# (scraper/src/routes/*.py, frontend/pdf_conv/*.py, frontend/maigret/*.py).
# The previous file was a 311-line `pip freeze` of the whole machine (Django,
# Wagtail, Poetry, semgrep, fyers_apiv3 … none of which this project uses).
# Pip will resolve transitive deps from these top-level pins.
#
# Note: `maigret` itself is run as an external CLI/server (frontend/maigret/),
# install it separately per its own docs.

# Telegram ingestion
telethon==1.36.0

# MongoDB driver (dnspython is required for mongodb+srv:// Atlas URIs)
pymongo==4.8.0
dnspython==2.6.1

# Report generation (PDF)
reportlab==4.2.2
PyPDF2==3.0.1

# Maigret / report web servers
flask==3.0.3
flask-cors==4.0.1

# Chatbot (Gemini via LangChain)
langchain==0.2.16
langchain-core==0.2.40
langchain-community==0.2.16
langchain-google-genai==1.0.10

# HTTP + config
requests==2.32.3
python-dotenv==1.0.1
